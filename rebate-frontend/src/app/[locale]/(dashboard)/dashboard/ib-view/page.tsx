'use client';

import { IbViewTree } from '@/components/ib-tree/IbViewTree';

export default function IbViewPage() {
  return (
    <div className="animate-in fade-in slide-in-from-bottom-4 duration-500">
      <IbViewTree />
    </div>
  );
}
